function findIndexOf(str, target) {
    //str.indexOf(target) give you the index number from starting 
    console.log("index of string :", str.indexOf(target));
    console.log("Original string :", str);
}
findIndexOf("Hello World", "World");